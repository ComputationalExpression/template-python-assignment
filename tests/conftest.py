import mockro

# Uncomment and adjust the overrides below to change how mocked hardware
# behaves during tests.
# mockro.patch("machine.Pin.value", return_value=1)
# mockro.patch("network.WLAN.isconnected", return_value=True)
